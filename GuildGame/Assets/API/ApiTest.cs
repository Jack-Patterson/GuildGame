using GuildGameTestLibrary;
using UnityEngine;

namespace com.Halcyon
{
    public class ApiTest : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Class1.Main(new []{""});
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
